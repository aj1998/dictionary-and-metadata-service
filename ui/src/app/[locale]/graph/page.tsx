export default function GraphPage() {
  return (
    <div className="flex h-full items-center justify-center text-foreground-muted text-[length:var(--font-size-body)]">
      {/* Phase 4: graph canvas */}
      ग्राफ — Phase 4
    </div>
  );
}
